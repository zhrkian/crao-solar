const styles = theme => ({
  root: { display: 'flex', alignItems: 'flex-start', width: '100%', justifyContent: 'space-between' },
  form: { width: '37%' },
  select: { width: '100%' },
})

export default styles
