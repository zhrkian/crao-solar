const styles = theme => ({
  root: {display: 'flex', alignItems: 'flex-end', width: '100%', justifyContent: 'space-between'},
  inputs: { width: '67%' },
  input: { marginBottom: 10 }
})

export default styles
